# Meeting Log — 2026-01-03

Sessione di pianificazione focalizzata sulla fase di validazione del simulatore PMCSN ASF e sulla produzione di prompt Codex per l’implementazione end-to-end.

---

## 1. Allineamento concettuale della validazione
- Ribadita la distinzione tra **verifica** (correttezza interna) e **validazione** (aderenza al sistema reale), mappando le fasi del workflow a formalismi MC/MS/MCMP e al ciclo DEV → REVIEW → TEST.
- Chiarita la coerenza semantica: backlog ≡ coda DEV; metriche di throughput/attesa/utilization restano invarianti rispetto alla semantica definita in precedenza.
- Concordata la necessità di tracciare tolleranze, monotonicità attese e requisiti di riproducibilità (semi deterministici, snapshot di config e parametri di stato) come parte della validazione.

## 2. Prompt Codex prodotti (suite 2026-01-03)
- **Prompt 1 — Validation charter & KPI mapping:** definisce il piano di validazione, l’allineamento KPI ↔ dati ETL e le tolleranze di accettazione; output atteso `docs/validation/validation_plan.md`.
- **Prompt 2 — Baseline data & empirical alignment:** estrazione di baseline empiriche da ETL con export di `validation/baseline_metrics.csv` e `validation/baseline_metadata.json`, più snapshot di configurazione e semi.
- **Prompt 3 — Validation harness & consistency checks:** progettazione della CLI `simulation.validate`/runner con scenari seedati, integrazione con `simulation.verify`, controlli di monotonicità/bounds/conservazione e report JSON/Markdown.
- **Prompt 4 — Distribution & parameter plausibility:** confronti tra parametri di servizio simulati e fit ETL (`fit_summary.csv`/`service_params.json`), con diagnostiche KS/CDF e segnalazione di drift.
- **Prompt 5 — Validation sweep specs & automation:** definizione di uno sweep `validation_monotonicity.csv` per scenari ARRIVAL_RATE/FEEDBACK/capacità, output isolati in `simulation/experiments/validation_monotonicity/` e report aggregato.
- **Prompt 6 — Reporting & workflow integration:** integrazione della validazione nella documentazione e nel workflow (README/guide), eventuale comando `make validate` o script dedicato, preservando compatibilità con la verifica esistente.

## 3. Decisioni e note operative
- Fonte canonica per la giornata: `docs/agents/codex_prompts/20260103_prompts_a.md`, che elenca obiettivi, file da leggere e deliverable per ogni prompt.
- Nessuna modifica al codice applicata oggi; il focus resta sulla progettazione dettagliata e sulla tracciabilità dei requisiti di validazione.

## 4. Prossimi passi
- Redigere/aggiornare `docs/validation/validation_plan.md` seguendo il Prompt 1, includendo mapping KPI ↔ ETL, tolleranze e aspettative di monotonicità.
- Implementare le estrazioni baseline (Prompt 2) e integrare il runner di validazione con scenari seedati e reportistica (Prompt 3).
- Estendere i controlli di plausibilità distributiva (Prompt 4), codificare lo sweep di validazione (Prompt 5) e aggiornare la documentazione/workflow con il comando di esecuzione unificato (Prompt 6).
